# Données runtime du presse-papier — SENSIBLE, ne jamais versionner
clipstack/

# Logs runtime
setup.log
*.log

# Backups horodatés
*.bak.*
*.backup.*

# Secrets locaux (dotfiles) — jamais versionnés, seul le .example l'est
.zshrc.local
zshrc.local

# macOS
.DS_Store
